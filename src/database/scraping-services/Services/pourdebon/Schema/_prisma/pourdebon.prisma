
generator client {
  provider = "prisma-client-js"
  output = env("POURDEBON_GEN_CLIENT_OUTPUT")
}

datasource db {
  provider = env("POURDEBON_DAT_DB_PROVIDER")
  url = env("POURDEBON_DAT_DB_URL")
}
