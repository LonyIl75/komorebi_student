generator client {
  provider = "prisma-client-js"
  output = env("ENTREPRISE__GEN_CLIENT_OUTPUT")
}

datasource db {
  provider = env("ENTREPRISE__DAT_DB_PROVIDER")
  url = env("ENTREPRISE__DAT_DB_URL")
}