generator client {
  provider = "prisma-client-js"
  output = env("BOOKSTOSCRAPE_GEN_CLIENT_OUTPUT")
}

datasource db {
  provider = env("BOOKSTOSCRAPE_DAT_DB_PROVIDER")
  url = env("BOOKSTOSCRAPE_DAT_DB_URL")
}